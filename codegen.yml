schema: http://localhost:3000/api/graphql

generates:
  src/generated/gen-types.ts:
    config:
      defaultMapper: any
      contextType: @typeDefs/resolver#ResolverContext
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers
